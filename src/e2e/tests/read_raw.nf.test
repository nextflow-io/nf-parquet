nextflow_pipeline {

    name "Test read raw"
    script "workflows/read_raw.nf"

    test("Should run without failures") {

        then {
            assert workflow.success
            assert snapshot(workflow.stdout).match()
        }

    }

}
