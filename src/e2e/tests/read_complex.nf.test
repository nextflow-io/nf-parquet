nextflow_pipeline {

    name "Test read complex"
    script "workflows/read_complex.nf"

    test("Should run without failures") {

        then {
            assert workflow.success
            assert snapshot(workflow.stdout).match()
        }

    }

}
