nextflow_pipeline {

    name "Test read"
    script "workflows/read.nf"

    test("Should run without failures") {

        then {
            assert workflow.success
            assert snapshot(workflow.stdout).match()
        }

    }

}
